<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mozi Foglalás</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5" style="max-width: 600px;">
        <h1>Mozi Foglalás</h1>
        <% if (typeof errors !== 'undefined' && errors.length > 0) { %>
            <div class="alert alert-danger">
                <ul>
                    <% errors.forEach(function(error) { %>
                        <li><%= error %></li>
                    <% }); %>
                </ul>
            </div>
        <% } %>
        <form method="POST">
            <div class="mb-3">
                <label for="name" class="form-label">Név</label>
                <input type="text" class="form-control" id="name" name="name" value="<%= typeof data !== 'undefined' ? data.name : '' %>" required>
            </div>
            <div class="mb-3">
                <label for="email" class="form-label">E-mail cím</label>
                <input type="email" class="form-control" id="email" name="email" value="<%= typeof data !== 'undefined' ? data.email : '' %>" required>
            </div>
            <div class="mb-3">
                <label for="dateTime" class="form-label">Dátum és időpont</label>
                <input type="datetime-local" class="form-control" id="dateTime" name="dateTime" value="<%= typeof data !== 'undefined' ? data.dateTime : '' %>" required>
            </div>
            <div class="mb-3">
                <label for="guests" class="form-label">Nézők száma</label>
                <input type="number" class="form-control" id="guests" name="guests" min="1" max="10" value="<%= typeof data !== 'undefined' ? data.guests : '' %>" required>
            </div>
            <button type="submit" class="btn btn-primary w-100">Foglalás</button>
        </form>
    </div>
</body>
</html>
